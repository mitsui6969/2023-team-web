// import { useContext } from "react";

export default function Header() {
    // const {isAuth, setIsAuth} = useContext(AuthFlagContext);
    // console.log(isAth);

    return (
        <>
            <p>header</p>

            <nav>
                <ul>
                    <link href="/src/App.jsx">Reactテンプレート</link>
                </ul>
            </nav>
        </>
    )
}